<div class="">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12 col-lg-12 d-flex align-items-center">
        <div class="card-body p-4 p-lg-5 text-black">

          <form action="/admin/edit-category/{{category._id}}" method="post">
          <div class="">
          
          </div>
            <div class="d-flex align-items-center mb-3 pb-1">
              <span class="h1 fw-bold mb-0">Edit Category</span>
            </div>
            
            <div class="form-outline mb-2">
                {{!-- {{#if catErr}}
                <p class="text-danger">{{catErr}}</p>
                {{/if}} --}}
                
              <input type="text" value="{{category.name}}" name="name" id="form2Example17" class="form-control form-control-lg text-capitalize" required/>
      
              <label class="form-label" for="form2Example17">Category Name</label>
            </div>
            <div class="pt-1">
              <button class="btn btn-dark btn-lg btn-block" type="submit">Submit</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>